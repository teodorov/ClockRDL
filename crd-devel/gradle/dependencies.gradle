//injected dependencies
subprojects {
	repositories {
		mavenCentral()
        maven {
            url project.localRepositoryPath
        }
	}

    dependencies {
        testCompile group: 'junit', name: 'junit', version: '4.+'
    }
}