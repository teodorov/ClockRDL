configurations { antlr4 }
dependencies {
	compile project(':crd-language')
	testCompile project (path: ":crd-language", configuration: 'tests')
}

clean << {
    project.delete "tmp"
}


test {
    outputs.upToDateWhen { false }
}